# list can ahve any type of data
# slicing, concatenate, repetition, in check and iterations
# min, max, cmp, append, count, insert, pop, removes, sort

lst = [1, 2, 3, 'a', 'b', 'c']
listOne = [0.1, 0.2, 0.4]

con = lst + listOne

for i in lst:
    print(i)

    # this is called iteration -> print the array item

    # Repeatation -> repeat the same hi in 4 element of array 
    a = ['hi']*4;

    print(a)  



